import type { RenderOptions } from '@vue-email/compiler';
/**
 * Compile a email template
 * @param {string} filename
 * @param {object} options
 * @returns {string}
 *
 * @example
 * ```ts
 * useCompiler('template.vue', {
 *  props: {
 *    name: 'foo',
 *  },
 *  locale: 'en',
 *  translations: {},
 * })
 * ```
 */
export declare function useCompiler(filename: string, data?: RenderOptions, verbose?: boolean): Promise<import("@vue-email/compiler/index").Result>;
